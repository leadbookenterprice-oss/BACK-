{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python -u manage.py migrate --noinput && if [ \"${APP_ROLE:-web}\" = \"worker\" ]; then exec celery -A subzero_core worker --loglevel=info --concurrency=1; else exec daphne -b 0.0.0.0 -p $PORT subzero_core.asgi:application; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
